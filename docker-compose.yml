services:
  server:
    build: ./server
    ports:
      - "4000:4000"
    environment:
      - PORT=4000
      - DATABASE_URL=file:/data/openlink.db
      - JWT_SECRET=${JWT_SECRET:?set a long random JWT_SECRET in a .env file}
    volumes:
      - openlink-data:/data

volumes:
  openlink-data:
